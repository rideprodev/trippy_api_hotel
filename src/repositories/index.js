import userRepository from "./userRepository";
import accountRepository from "./accountRepository";

export default {
  userRepository,
  accountRepository,
  airportRepository,
  airlineRepository,
  membersRepository,
};
